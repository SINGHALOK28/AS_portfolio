"use client";

import React, { useState, useEffect } from "react";
import { usePortfolioConfig } from "@/context/PortfolioConfigContext";
import { playClickSound, playXpSound } from "@/utils/soundManager";
import {
  Save,
  RefreshCw,
  Download,
  ArrowLeft,
  Plus,
  Trash2,
  ExternalLink,
  User,
  GraduationCap,
  Sparkles,
  Award,
  BookOpen,
  Image as ImageIcon,
  Briefcase,
  Layers,
  Terminal,
  Settings,
  HelpCircle,
  FileText
} from "lucide-react";
import Link from "next/link";

type EditableConfig = Omit<ReturnType<typeof usePortfolioConfig>["config"], "projects"> & {
  projects: (Omit<ReturnType<typeof usePortfolioConfig>["config"]["projects"][number], "metrics"> & {
    metrics: Record<string, string>;
  })[];
};

export default function AdminPage() {
  const { config, updateConfig, resetConfig, isLoaded } = usePortfolioConfig();
  const [activeTab, setActiveTab] = useState("profile");
  const [editConfig, setEditConfig] = useState<EditableConfig | null>(null);
  const [toastMessage, setToastMessage] = useState<string | null>(null);
  const [toastType, setToastType] = useState<"success" | "info" | "error">("success");

  // Load configuration into local editing state once loaded
  useEffect(() => {
    if (isLoaded && config) {
      setEditConfig(JSON.parse(JSON.stringify(config)));
    }
  }, [isLoaded, config]);

  const triggerToast = (msg: string, type: "success" | "info" | "error" = "success") => {
    setToastMessage(msg);
    setToastType(type);
    setTimeout(() => {
      setToastMessage(null);
    }, 4000);
  };

  if (!isLoaded || !editConfig) {
    return (
      <div className="min-h-screen bg-[#070b0c] text-white flex flex-col items-center justify-center font-mono">
        <div className="w-12 h-12 border-4 border-emerald border-t-transparent rounded-full animate-spin mb-4"></div>
        <p className="text-sm uppercase tracking-wider text-emerald animate-pulse">
          Hydrating Voxel Config Memory...
        </p>
      </div>
    );
  }

  // Action handlers
  const handleSave = () => {
    playXpSound();
    updateConfig(editConfig as any);
    triggerToast("Config synced to LocalStorage successfully!", "success");
  };

  const handleReset = () => {
    playClickSound();
    if (window.confirm("Are you sure you want to discard all changes and reset to defaults?")) {
      resetConfig();
      setEditConfig(JSON.parse(JSON.stringify(config)));
      triggerToast("Config reset to static defaults.", "info");
    }
  };

  const handleExport = () => {
    playXpSound();
    const configString = `// THIS FILE IS AUTO-GENERATED BY THE PORTFOLIO ADMIN PANEL
// Copy and paste this file into your project at: src/config/userConfig.ts

export const USER_CONFIG = ${JSON.stringify(editConfig, null, 2)};
`;
    const blob = new Blob([configString], { type: "text/typescript;charset=utf-8;" });
    const url = URL.createObjectURL(blob);
    const link = document.createElement("a");
    link.href = url;
    link.setAttribute("download", "userConfig.ts");
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    triggerToast("Downloaded userConfig.ts! Place it in src/config/ to lock it in.", "success");
  };

  // Helper updates
  const updateProfileField = (field: keyof typeof editConfig.profile, value: any) => {
    setEditConfig((prev) => {
      if (!prev) return null;
      return {
        ...prev,
        profile: {
          ...prev.profile,
          [field]: value,
        },
      };
    });
  };

  const updateUsernameField = (field: keyof typeof editConfig.usernames, value: string) => {
    setEditConfig((prev) => {
      if (!prev) return null;
      return {
        ...prev,
        usernames: {
          ...prev.usernames,
          [field]: value,
        },
      };
    });
  };

  const updateFallbackStatField = (platform: keyof typeof editConfig.fallbackStats, field: string, value: any) => {
    setEditConfig((prev) => {
      if (!prev) return null;
      return {
        ...prev,
        fallbackStats: {
          ...prev.fallbackStats,
          [platform]: {
            ...prev.fallbackStats[platform],
            [field]: value,
          },
        },
      };
    });
  };

  return (
    <div className="min-h-screen bg-[#070b0c] text-white flex flex-col font-sans selection:bg-emerald selection:text-black">
      
      {/* Dynamic Header Dashboard Control Bar */}
      <header className="sticky top-0 z-40 bg-[#070b0c]/90 backdrop-blur-md border-b border-emerald/20 px-6 py-4 flex flex-col sm:flex-row items-center justify-between gap-4">
        <div className="flex items-center space-x-3">
          <div className="p-2 bg-emerald/10 border border-emerald/30 rounded-lg text-emerald">
            <Terminal className="w-5 h-5" />
          </div>
          <div>
            <h1 className="font-pixel text-[11px] text-emerald uppercase tracking-widest leading-none">
              ADMIN_SHELL_v1.0
            </h1>
            <h2 className="font-mono text-lg font-bold text-white leading-tight">
              Portfolio Control Center
            </h2>
          </div>
        </div>

        {/* Global Controls */}
        <div className="flex items-center flex-wrap gap-2.5">
          <Link
            href="/"
            onClick={playClickSound}
            className="px-4 py-2 border border-gray-800 hover:border-white rounded-lg text-xs font-mono transition-colors flex items-center gap-1.5"
          >
            <ArrowLeft className="w-3.5 h-3.5" />
            <span>PORTFOLIO</span>
          </Link>
          <button
            onClick={handleReset}
            className="px-4 py-2 border border-red-500/25 hover:border-red-500 hover:bg-red-500/5 rounded-lg text-xs font-mono text-red-400 transition-all flex items-center gap-1.5"
            title="Reset storage configurations to userConfig.ts static code defaults"
          >
            <RefreshCw className="w-3.5 h-3.5" />
            <span>RESET</span>
          </button>
          <button
            onClick={handleSave}
            className="px-4 py-2 bg-emerald hover:bg-emerald/90 text-black border border-black rounded-lg text-xs font-pixel uppercase tracking-wide transition-all shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
          >
            <Save className="w-3.5 h-3.5" />
            <span>APPLY & SAVE</span>
          </button>
          <button
            onClick={handleExport}
            className="px-4 py-2 bg-purple-glow hover:bg-purple-glow/90 text-white border border-black rounded-lg text-xs font-pixel uppercase tracking-wide transition-all shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
            title="Generate and download source-code userConfig.ts file"
          >
            <Download className="w-3.5 h-3.5" />
            <span>EXPORT TS</span>
          </button>
        </div>
      </header>

      {/* Main Admin layout body */}
      <div className="flex-1 flex flex-col md:flex-row items-stretch">
        
        {/* Left Side Tab Navigation Panels */}
        <nav className="w-full md:w-64 border-r border-emerald/10 bg-[#090e0f] p-4 flex flex-row md:flex-col gap-1.5 overflow-x-auto md:overflow-x-visible shrink-0 scrollbar-thin">
          {[
            { id: "profile", label: "Profile Specs", icon: User },
            { id: "socials", label: "APIs & Socials", icon: Settings },
            { id: "education", label: "Education Path", icon: GraduationCap },
            { id: "skills", label: "Skills Matrix", icon: Sparkles },
            { id: "experience", label: "Work Experience", icon: Briefcase },
            { id: "projects", label: "Project Archives", icon: Layers },
            { id: "certs", label: "Credentials & Medals", icon: Award },
            { id: "blogs", label: "Blogs & Scrolls", icon: BookOpen },
            { id: "gallery", label: "Memory Gallery", icon: ImageIcon }
          ].map((tab) => {
            const Icon = tab.icon;
            const isActive = activeTab === tab.id;
            return (
              <button
                key={tab.id}
                onClick={() => {
                  playClickSound();
                  setActiveTab(tab.id);
                }}
                className={`flex items-center gap-3 px-4 py-3 rounded-lg text-xs font-mono transition-all whitespace-nowrap text-left border ${
                  isActive
                    ? "bg-emerald/10 border-emerald/30 text-emerald font-bold"
                    : "border-transparent text-gray-400 hover:text-white hover:bg-white/5"
                }`}
              >
                <Icon className={`w-4 h-4 shrink-0 ${isActive ? "text-emerald animate-pulse" : "text-gray-500"}`} />
                <span>{tab.label}</span>
              </button>
            );
          })}
        </nav>

        {/* Right Side Config Form Workspace Panels */}
        <main className="flex-1 p-6 md:p-10 max-w-5xl overflow-y-auto">
          
          {/* Active Tab rendering */}
          {activeTab === "profile" && (
            <div className="space-y-6">
              <div>
                <h3 className="font-mono text-lg font-bold text-emerald">Profile Settings</h3>
                <p className="font-sans text-xs text-gray-500 mt-1">
                  Configure your primary display name, job status role badges, profile avatar, and bio details.
                </p>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-2 gap-6 bg-[#0a0f11] border border-gray-800 rounded-xl p-6">
                <div className="space-y-1">
                  <label className="font-mono text-xs text-gray-400 uppercase">Display Name</label>
                  <input
                    type="text"
                    value={editConfig.profile.name}
                    onChange={(e) => updateProfileField("name", e.target.value)}
                    className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-4 py-2 text-sm font-mono focus:outline-none transition-colors"
                  />
                </div>
                
                <div className="space-y-1">
                  <label className="font-mono text-xs text-gray-400 uppercase">Headline Title</label>
                  <input
                    type="text"
                    value={editConfig.profile.title}
                    onChange={(e) => updateProfileField("title", e.target.value)}
                    className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-4 py-2 text-sm font-mono focus:outline-none transition-colors"
                  />
                </div>

                <div className="space-y-1">
                  <label className="font-mono text-xs text-gray-400 uppercase">Email Address</label>
                  <input
                    type="email"
                    value={editConfig.profile.email}
                    onChange={(e) => updateProfileField("email", e.target.value)}
                    className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-4 py-2 text-sm font-mono focus:outline-none transition-colors"
                  />
                </div>

                <div className="space-y-1">
                  <label className="font-mono text-xs text-gray-400 uppercase">Phone Number</label>
                  <input
                    type="text"
                    value={editConfig.profile.phone}
                    onChange={(e) => updateProfileField("phone", e.target.value)}
                    className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-4 py-2 text-sm font-mono focus:outline-none transition-colors"
                  />
                </div>

                <div className="space-y-1">
                  <label className="font-mono text-xs text-gray-400 uppercase">Avatar Image Path</label>
                  <input
                    type="text"
                    value={editConfig.profile.avatarUrl}
                    onChange={(e) => updateProfileField("avatarUrl", e.target.value)}
                    className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-4 py-2 text-sm font-mono focus:outline-none transition-colors"
                  />
                </div>

                <div className="space-y-1">
                  <label className="font-mono text-xs text-gray-400 uppercase">Resume URL</label>
                  <input
                    type="text"
                    value={editConfig.profile.resumeUrl}
                    onChange={(e) => updateProfileField("resumeUrl", e.target.value)}
                    className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-4 py-2 text-sm font-mono focus:outline-none transition-colors"
                  />
                </div>

                <div className="md:col-span-2 space-y-1">
                  <label className="font-mono text-xs text-gray-400 uppercase">Bio Introduction</label>
                  <textarea
                    rows={4}
                    value={editConfig.profile.bio}
                    onChange={(e) => updateProfileField("bio", e.target.value)}
                    className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg p-4 text-sm font-sans focus:outline-none transition-colors resize-none"
                  />
                </div>

                {/* Sub-list editor: profile roles */}
                <div className="md:col-span-2 space-y-3 pt-2">
                  <div className="flex justify-between items-center">
                    <label className="font-mono text-xs text-gray-400 uppercase">Typing Roles</label>
                    <button
                      onClick={() => {
                        playClickSound();
                        setEditConfig((prev) => {
                          if (!prev) return null;
                          return {
                            ...prev,
                            profile: {
                              ...prev.profile,
                              roles: [...prev.profile.roles, "New Role Banner"],
                            },
                          };
                        });
                      }}
                      className="px-2.5 py-1 bg-emerald/10 border border-emerald/20 hover:border-emerald/50 rounded text-[10px] font-mono text-emerald flex items-center gap-1"
                    >
                      <Plus className="w-3 h-3" />
                      <span>ADD ROLE</span>
                    </button>
                  </div>
                  
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
                    {editConfig.profile.roles.map((role, idx) => (
                      <div key={idx} className="flex items-center gap-2">
                        <input
                          type="text"
                          value={role}
                          onChange={(e) => {
                            const newRoles = [...editConfig.profile.roles];
                            newRoles[idx] = e.target.value;
                            updateProfileField("roles", newRoles);
                          }}
                          className="flex-1 bg-black/50 border border-gray-800 focus:border-emerald rounded px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                        <button
                          onClick={() => {
                            playClickSound();
                            const newRoles = editConfig.profile.roles.filter((_, i) => i !== idx);
                            updateProfileField("roles", newRoles);
                          }}
                          className="p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded transition-colors"
                        >
                          <Trash2 className="w-3.5 h-3.5" />
                        </button>
                      </div>
                    ))}
                  </div>
                </div>

              </div>
            </div>
          )}

          {activeTab === "socials" && (
            <div className="space-y-8">
              <div>
                <h3 className="font-mono text-lg font-bold text-emerald">Social Coordinates & Platform APIs</h3>
                <p className="font-sans text-xs text-gray-500 mt-1">
                  Input API handles for GitHub, LeetCode, Codeforces, etc. The platform pulls stats directly or simulates metrics using configured fallbacks.
                </p>
              </div>

              {/* API usernames */}
              <div className="space-y-4">
                <h4 className="font-mono text-xs text-gray-400 uppercase tracking-widest border-b border-gray-800 pb-2">// API_USERNAMES</h4>
                <div className="grid grid-cols-1 md:grid-cols-3 gap-6 bg-[#0a0f11] border border-gray-800 rounded-xl p-6">
                  {Object.keys(editConfig.usernames).map((platform) => (
                    <div key={platform} className="space-y-1">
                      <label className="font-mono text-xs text-gray-400 uppercase capitalize">{platform}</label>
                      <input
                        type="text"
                        value={editConfig.usernames[platform as keyof typeof editConfig.usernames] || ""}
                        onChange={(e) => updateUsernameField(platform as keyof typeof editConfig.usernames, e.target.value)}
                        className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-4 py-2 text-sm font-mono focus:outline-none transition-colors"
                      />
                    </div>
                  ))}
                </div>
              </div>

              {/* Fallback Stats metrics */}
              <div className="space-y-4">
                <h4 className="font-mono text-xs text-gray-400 uppercase tracking-widest border-b border-gray-800 pb-2">// OFFLINE_FALLBACK_METRICS</h4>
                <div className="space-y-6">
                  {Object.keys(editConfig.fallbackStats).map((platform) => {
                    const statsObj = editConfig.fallbackStats[platform as keyof typeof editConfig.fallbackStats];
                    return (
                      <div key={platform} className="bg-[#0a0f11] border border-gray-800 rounded-xl p-6">
                        <h5 className="font-mono text-sm font-bold text-white capitalize mb-4 flex items-center gap-2">
                          <span className="w-2 h-2 rounded bg-purple-glow"></span>
                          {platform} Fallbacks
                        </h5>
                        <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
                          {Object.keys(statsObj).map((key) => {
                            if (key === "contributions" || key === "badges") return null;
                            const value = statsObj[key as keyof typeof statsObj];
                            const isNumeric = typeof value === "number";
                            return (
                              <div key={key} className="space-y-1">
                                <label className="font-mono text-[10px] text-gray-500 uppercase">{key}</label>
                                <input
                                  type={isNumeric ? "number" : "text"}
                                  value={value as any}
                                  onChange={(e) => {
                                    const parsedVal = isNumeric ? Number(e.target.value) : e.target.value;
                                    updateFallbackStatField(
                                      platform as keyof typeof editConfig.fallbackStats,
                                      key,
                                      parsedVal
                                    );
                                  }}
                                  className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono focus:outline-none transition-colors"
                                />
                              </div>
                            );
                          })}
                        </div>
                      </div>
                    );
                  })}
                </div>
              </div>
            </div>
          )}

          {activeTab === "education" && (
            <div className="space-y-6">
              <div className="flex justify-between items-center">
                <div>
                  <h3 className="font-mono text-lg font-bold text-emerald">Education Milestones</h3>
                  <p className="font-sans text-xs text-gray-500 mt-1">
                    Manage degrees, universities, GPAs, and core courses mapped in the resume database.
                  </p>
                </div>
                <button
                  onClick={() => {
                    playClickSound();
                    setEditConfig((prev) => {
                      if (!prev) return null;
                      return {
                        ...prev,
                        education: [
                          ...prev.education,
                          {
                            degree: "New Degree Title",
                            specialization: "Specialization Track",
                            institution: "Institution/University Name",
                            duration: "2024 - 2028",
                            cgpa: "9.0 CGPA",
                            courses: ["Core Course 1"],
                          },
                        ],
                      };
                    });
                  }}
                  className="px-3 py-1.5 bg-emerald text-black font-pixel text-[10px] uppercase rounded-lg shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
                >
                  <Plus className="w-3.5 h-3.5" />
                  <span>ADD ENTRY</span>
                </button>
              </div>

              <div className="space-y-6">
                {editConfig.education.map((edu, idx) => (
                  <div key={idx} className="relative bg-[#0a0f11] border border-gray-800 hover:border-emerald/20 rounded-xl p-6 space-y-4">
                    <button
                      onClick={() => {
                        playClickSound();
                        setEditConfig((prev) => {
                          if (!prev) return null;
                          return {
                            ...prev,
                            education: prev.education.filter((_, i) => i !== idx),
                          };
                        });
                      }}
                      className="absolute top-4 right-4 p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded-lg transition-colors"
                      title="Delete education milestone"
                    >
                      <Trash2 className="w-4 h-4" />
                    </button>

                    <h4 className="font-pixel text-[9px] text-emerald uppercase tracking-wider mb-2">
                      // ED_ENTRY_{idx + 1}
                    </h4>

                    <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Degree/Certificate</label>
                        <input
                          type="text"
                          value={edu.degree}
                          onChange={(e) => {
                            const newEdu = [...editConfig.education];
                            newEdu[idx].degree = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, education: newEdu } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Specialization Track</label>
                        <input
                          type="text"
                          value={edu.specialization}
                          onChange={(e) => {
                            const newEdu = [...editConfig.education];
                            newEdu[idx].specialization = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, education: newEdu } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Institution Name</label>
                        <input
                          type="text"
                          value={edu.institution}
                          onChange={(e) => {
                            const newEdu = [...editConfig.education];
                            newEdu[idx].institution = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, education: newEdu } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="grid grid-cols-2 gap-3">
                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Duration</label>
                          <input
                            type="text"
                            value={edu.duration}
                            onChange={(e) => {
                              const newEdu = [...editConfig.education];
                              newEdu[idx].duration = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, education: newEdu } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Grade/GPA</label>
                          <input
                            type="text"
                            value={edu.cgpa}
                            onChange={(e) => {
                              const newEdu = [...editConfig.education];
                              newEdu[idx].cgpa = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, education: newEdu } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>
                      </div>
                    </div>

                    {/* Education Courses tags list */}
                    <div className="space-y-2 pt-2">
                      <div className="flex justify-between items-center">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Mapped Core Courses</label>
                        <button
                          onClick={() => {
                            playClickSound();
                            const newEdu = [...editConfig.education];
                            newEdu[idx].courses = [...newEdu[idx].courses, "New Course Module"];
                            setEditConfig(prev => prev ? { ...prev, education: newEdu } : null);
                          }}
                          className="px-2 py-0.5 bg-emerald/10 border border-emerald/20 hover:border-emerald/40 rounded text-[9px] font-mono text-emerald flex items-center gap-1"
                        >
                          <Plus className="w-3 h-3" />
                          <span>ADD COURSE</span>
                        </button>
                      </div>

                      <div className="flex flex-wrap gap-2">
                        {edu.courses.map((course, cIdx) => (
                          <div key={cIdx} className="flex items-center bg-black/60 border border-gray-800 rounded pl-2.5 pr-1.5 py-1">
                            <input
                              type="text"
                              value={course}
                              onChange={(e) => {
                                const newEdu = [...editConfig.education];
                                newEdu[idx].courses[cIdx] = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, education: newEdu } : null);
                              }}
                              className="bg-transparent border-none text-xs font-mono text-white focus:outline-none w-32 md:w-44 p-0"
                            />
                            <button
                              onClick={() => {
                                playClickSound();
                                const newEdu = [...editConfig.education];
                                newEdu[idx].courses = newEdu[idx].courses.filter((_, i) => i !== cIdx);
                                setEditConfig(prev => prev ? { ...prev, education: newEdu } : null);
                              }}
                              className="ml-2 text-gray-500 hover:text-red-400 p-0.5"
                            >
                              <Trash2 className="w-3 h-3" />
                            </button>
                          </div>
                        ))}
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}

          {activeTab === "skills" && (
            <div className="space-y-8">
              {/* Skill Categories Section */}
              <div className="space-y-4">
                <div className="flex justify-between items-center">
                  <div>
                    <h3 className="font-mono text-lg font-bold text-emerald">Skill Categories</h3>
                    <p className="font-sans text-xs text-gray-500 mt-1">
                      Organize competency matrix areas (e.g. Languages, Web Tools, Machine Learning).
                    </p>
                  </div>
                  <button
                    onClick={() => {
                      playClickSound();
                      setEditConfig((prev) => {
                        if (!prev) return null;
                        return {
                          ...prev,
                          skills: {
                            ...prev.skills,
                            categories: [
                              ...prev.skills.categories,
                              { id: "new-category", name: "New Category" }
                            ]
                          }
                        };
                      });
                    }}
                    className="px-2.5 py-1 bg-emerald/10 border border-emerald/20 hover:border-emerald/50 rounded text-[10px] font-mono text-emerald flex items-center gap-1"
                  >
                    <Plus className="w-3 h-3" />
                    <span>ADD CATEGORY</span>
                  </button>
                </div>

                <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4 bg-[#0a0f11] border border-gray-800 rounded-xl p-6">
                  {editConfig.skills.categories.map((cat, idx) => (
                    <div key={idx} className="flex gap-2 items-center bg-black/40 border border-gray-800 rounded-lg p-2.5">
                      <div className="flex-1 space-y-1">
                        <input
                          type="text"
                          value={cat.id}
                          placeholder="Category ID"
                          onChange={(e) => {
                            const newCats = [...editConfig.skills.categories];
                            newCats[idx].id = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, categories: newCats } } : null);
                          }}
                          className="w-full bg-transparent text-[10px] font-mono text-gray-500 uppercase border-b border-gray-900 focus:border-emerald outline-none pb-0.5"
                        />
                        <input
                          type="text"
                          value={cat.name}
                          placeholder="Category Name"
                          onChange={(e) => {
                            const newCats = [...editConfig.skills.categories];
                            newCats[idx].name = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, categories: newCats } } : null);
                          }}
                          className="w-full bg-transparent text-xs font-mono text-white outline-none"
                        />
                      </div>
                      <button
                        onClick={() => {
                          playClickSound();
                          const newCats = editConfig.skills.categories.filter((_, i) => i !== idx);
                          setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, categories: newCats } } : null);
                        }}
                        className="p-1.5 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded transition-colors"
                      >
                        <Trash2 className="w-3.5 h-3.5" />
                      </button>
                    </div>
                  ))}
                </div>
              </div>

              {/* Skills Items section */}
              <div className="space-y-4">
                <div className="flex justify-between items-center">
                  <div>
                    <h3 className="font-mono text-lg font-bold text-emerald">Competency Badges & Values</h3>
                    <p className="font-sans text-xs text-gray-500 mt-1">
                      Customize skill values (0-100), proficiency level tags, tech descriptions, and icon bindings.
                    </p>
                  </div>
                  <button
                    onClick={() => {
                      playClickSound();
                      setEditConfig((prev) => {
                        if (!prev) return null;
                        return {
                          ...prev,
                          skills: {
                            ...prev.skills,
                            items: [
                              ...prev.skills.items,
                              {
                                name: "New Skill",
                                category: editConfig.skills.categories[0]?.id || "languages",
                                level: "Intermediate",
                                value: 75,
                                icon: "code",
                                desc: "Short skill description log"
                              }
                            ]
                          }
                        };
                      });
                    }}
                    className="px-3 py-1.5 bg-emerald text-black font-pixel text-[10px] uppercase rounded-lg shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
                  >
                    <Plus className="w-3.5 h-3.5" />
                    <span>ADD SKILL</span>
                  </button>
                </div>

                <div className="space-y-4 max-h-[600px] overflow-y-auto border border-gray-800 rounded-xl p-4 bg-[#0a0f11] scrollbar-thin">
                  {editConfig.skills.items.map((skill, idx) => (
                    <div key={idx} className="relative bg-black/40 border border-gray-900 rounded-lg p-4 grid grid-cols-1 md:grid-cols-12 gap-4 items-center">
                      <button
                        onClick={() => {
                          playClickSound();
                          const newItems = editConfig.skills.items.filter((_, i) => i !== idx);
                          setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, items: newItems } } : null);
                        }}
                        className="absolute top-2 right-2 p-1 text-gray-500 hover:text-red-400 transition-colors"
                        title="Delete skill"
                      >
                        <Trash2 className="w-4 h-4" />
                      </button>

                      {/* Details inputs */}
                      <div className="md:col-span-3 space-y-1">
                        <label className="font-mono text-[9px] text-gray-500 uppercase">Skill Name</label>
                        <input
                          type="text"
                          value={skill.name}
                          onChange={(e) => {
                            const newItems = [...editConfig.skills.items];
                            newItems[idx].name = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, items: newItems } } : null);
                          }}
                          className="w-full bg-black/60 border border-gray-800 focus:border-emerald rounded px-2.5 py-1 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="md:col-span-2 space-y-1">
                        <label className="font-mono text-[9px] text-gray-500 uppercase">Category</label>
                        <select
                          value={skill.category}
                          onChange={(e) => {
                            const newItems = [...editConfig.skills.items];
                            newItems[idx].category = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, items: newItems } } : null);
                          }}
                          className="w-full bg-black/60 border border-gray-800 focus:border-emerald rounded px-2.5 py-1 text-xs font-mono text-white outline-none"
                        >
                          {editConfig.skills.categories.map((c) => (
                            <option key={c.id} value={c.id}>
                              {c.name}
                            </option>
                          ))}
                        </select>
                      </div>

                      <div className="md:col-span-2 space-y-1">
                        <label className="font-mono text-[9px] text-gray-500 uppercase">Level Tag</label>
                        <input
                          type="text"
                          value={skill.level}
                          onChange={(e) => {
                            const newItems = [...editConfig.skills.items];
                            newItems[idx].level = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, items: newItems } } : null);
                          }}
                          className="w-full bg-black/60 border border-gray-800 focus:border-emerald rounded px-2.5 py-1 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="md:col-span-2 space-y-1">
                        <div className="flex justify-between font-mono text-[9px] text-gray-500 uppercase">
                          <span>Value (%)</span>
                          <span className="text-emerald">{skill.value}%</span>
                        </div>
                        <input
                          type="range"
                          min="0"
                          max="100"
                          value={skill.value}
                          onChange={(e) => {
                            const newItems = [...editConfig.skills.items];
                            newItems[idx].value = Number(e.target.value);
                            setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, items: newItems } } : null);
                          }}
                          className="w-full h-1.5 bg-gray-800 rounded-lg appearance-none cursor-pointer accent-emerald"
                        />
                      </div>

                      <div className="md:col-span-3 space-y-1">
                        <label className="font-mono text-[9px] text-gray-500 uppercase">Description Logs</label>
                        <input
                          type="text"
                          value={skill.desc}
                          onChange={(e) => {
                            const newItems = [...editConfig.skills.items];
                            newItems[idx].desc = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, skills: { ...prev.skills, items: newItems } } : null);
                          }}
                          className="w-full bg-black/60 border border-gray-800 focus:border-emerald rounded px-2.5 py-1 text-xs font-sans text-gray-300 outline-none"
                        />
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          )}

          {activeTab === "experience" && (
            <div className="space-y-6">
              <div className="flex justify-between items-center">
                <div>
                  <h3 className="font-mono text-lg font-bold text-emerald">Work History & Experiences</h3>
                  <p className="font-sans text-xs text-gray-500 mt-1">
                    Add or update your internships, collaborative roles, and institutional developer leadership positions.
                  </p>
                </div>
                <button
                  onClick={() => {
                    playClickSound();
                    setEditConfig((prev) => {
                      if (!prev) return null;
                      return {
                        ...prev,
                        experiences: [
                          ...prev.experiences,
                          {
                            role: "Associate Developer Intern",
                            company: "Company / Division Name",
                            duration: "Summer 2026",
                            achievements: ["Collaborated with product teams to build core modules."],
                            techs: ["Python", "SQL"]
                          }
                        ]
                      };
                    });
                  }}
                  className="px-3 py-1.5 bg-emerald text-black font-pixel text-[10px] uppercase rounded-lg shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
                >
                  <Plus className="w-3.5 h-3.5" />
                  <span>ADD JOB</span>
                </button>
              </div>

              <div className="space-y-8">
                {editConfig.experiences.map((exp, idx) => (
                  <div key={idx} className="relative bg-[#0a0f11] border border-gray-800 rounded-xl p-6 space-y-6">
                    <button
                      onClick={() => {
                        playClickSound();
                        setEditConfig((prev) => {
                          if (!prev) return null;
                          return {
                            ...prev,
                            experiences: prev.experiences.filter((_, i) => i !== idx)
                          };
                        });
                      }}
                      className="absolute top-4 right-4 p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded-lg transition-colors"
                    >
                      <Trash2 className="w-4 h-4" />
                    </button>

                    <h4 className="font-pixel text-[9px] text-emerald uppercase tracking-wider">
                      // JOB_ARCHIVE_{idx + 1}
                    </h4>

                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Role / Position</label>
                        <input
                          type="text"
                          value={exp.role}
                          onChange={(e) => {
                            const newExps = [...editConfig.experiences];
                            newExps[idx].role = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Company / Organization</label>
                        <input
                          type="text"
                          value={exp.company}
                          onChange={(e) => {
                            const newExps = [...editConfig.experiences];
                            newExps[idx].company = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Duration</label>
                        <input
                          type="text"
                          value={exp.duration}
                          onChange={(e) => {
                            const newExps = [...editConfig.experiences];
                            newExps[idx].duration = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>
                    </div>

                    {/* Tech stack tags */}
                    <div className="space-y-2">
                      <div className="flex justify-between items-center">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Technologies Used</label>
                        <button
                          onClick={() => {
                            playClickSound();
                            const newExps = [...editConfig.experiences];
                            newExps[idx].techs = [...newExps[idx].techs, "New Tech"];
                            setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                          }}
                          className="px-2 py-0.5 bg-emerald/10 border border-emerald/20 rounded text-[9px] font-mono text-emerald flex items-center gap-1"
                        >
                          <Plus className="w-3 h-3" /> Add Tech
                        </button>
                      </div>

                      <div className="flex flex-wrap gap-2">
                        {exp.techs.map((tech, tIdx) => (
                          <div key={tIdx} className="flex items-center bg-black/60 border border-gray-800 rounded pl-2.5 pr-1.5 py-1">
                            <input
                              type="text"
                              value={tech}
                              onChange={(e) => {
                                const newExps = [...editConfig.experiences];
                                newExps[idx].techs[tIdx] = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                              }}
                              className="bg-transparent border-none text-xs font-mono text-white focus:outline-none w-20 p-0"
                            />
                            <button
                              onClick={() => {
                                playClickSound();
                                const newExps = [...editConfig.experiences];
                                newExps[idx].techs = newExps[idx].techs.filter((_, i) => i !== tIdx);
                                setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                              }}
                              className="ml-2 text-gray-500 hover:text-red-400 p-0.5"
                            >
                              <Trash2 className="w-3 h-3" />
                            </button>
                          </div>
                        ))}
                      </div>
                    </div>

                    {/* Achievements bullet editor */}
                    <div className="space-y-3">
                      <div className="flex justify-between items-center">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Core Deliverables & Highlights</label>
                        <button
                          onClick={() => {
                            playClickSound();
                            const newExps = [...editConfig.experiences];
                            newExps[idx].achievements = [...newExps[idx].achievements, "Brief bullet log outline of achievement deliverables"];
                            setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                          }}
                          className="px-2 py-0.5 bg-emerald/10 border border-emerald/20 rounded text-[9px] font-mono text-emerald flex items-center gap-1"
                        >
                          <Plus className="w-3 h-3" /> Add Bullet
                        </button>
                      </div>

                      <div className="space-y-2">
                        {exp.achievements.map((bullet, bIdx) => (
                          <div key={bIdx} className="flex gap-2">
                            <span className="text-emerald font-mono mt-2 shrink-0">▸</span>
                            <textarea
                              rows={1}
                              value={bullet}
                              onChange={(e) => {
                                const newExps = [...editConfig.experiences];
                                newExps[idx].achievements[bIdx] = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                              }}
                              className="flex-1 bg-black/40 border border-gray-800 focus:border-emerald rounded p-2 text-xs font-sans text-gray-200 focus:outline-none resize-none"
                            />
                            <button
                              onClick={() => {
                                playClickSound();
                                const newExps = [...editConfig.experiences];
                                newExps[idx].achievements = newExps[idx].achievements.filter((_, i) => i !== bIdx);
                                setEditConfig(prev => prev ? { ...prev, experiences: newExps } : null);
                              }}
                              className="p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded transition-colors h-9 flex items-center"
                            >
                              <Trash2 className="w-3.5 h-3.5" />
                            </button>
                          </div>
                        ))}
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}

          {activeTab === "projects" && (
            <div className="space-y-6">
              <div className="flex justify-between items-center">
                <div>
                  <h3 className="font-mono text-lg font-bold text-emerald">Project Archives</h3>
                  <p className="font-sans text-xs text-gray-500 mt-1">
                    Update showcase project records, metrics dashboards, Git links, and deployment statistics.
                  </p>
                </div>
                <button
                  onClick={() => {
                    playClickSound();
                    setEditConfig((prev) => {
                      if (!prev) return null;
                      return {
                        ...prev,
                        projects: [
                          ...prev.projects,
                          {
                            id: "new-project",
                            title: "New Project Showcase",
                            category: "Full-Stack",
                            shortDesc: "Short grid summary card tagline",
                            longDesc: "Deep-dive details about project architecture layout and mechanics.",
                            techStack: ["React", "Python"],
                            github: "https://github.com/alok-singh",
                            demo: "https://github.com/alok-singh",
                            featured: false,
                            status: "Development Phase",
                            metrics: { resolutionSpeed: "90%" },
                            challenges: "Primary challenge metadata logs here.",
                            learnings: "Key insights, skills gained, and engineering takeaways.",
                            architectureUrl: ""
                          } as any
                        ]
                      } as any;
                    });
                  }}
                  className="px-3 py-1.5 bg-emerald text-black font-pixel text-[10px] uppercase rounded-lg shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
                >
                  <Plus className="w-3.5 h-3.5" />
                  <span>ADD PROJECT</span>
                </button>
              </div>

              <div className="space-y-10">
                {editConfig.projects.map((proj, idx) => (
                  <div key={idx} className="relative bg-[#0a0f11] border border-gray-800 rounded-xl p-6 space-y-5">
                    <button
                      onClick={() => {
                        playClickSound();
                        setEditConfig((prev) => {
                          if (!prev) return null;
                          return {
                            ...prev,
                            projects: prev.projects.filter((_, i) => i !== idx)
                          };
                        });
                      }}
                      className="absolute top-4 right-4 p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded-lg transition-colors"
                    >
                      <Trash2 className="w-4 h-4" />
                    </button>

                    <h4 className="font-pixel text-[9px] text-emerald uppercase tracking-wider">
                      // PROJECT_{idx + 1} ({proj.id})
                    </h4>

                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Project Slug ID</label>
                        <input
                          type="text"
                          value={proj.id}
                          onChange={(e) => {
                            const newProjs = [...editConfig.projects];
                            newProjs[idx].id = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Project Title</label>
                        <input
                          type="text"
                          value={proj.title}
                          onChange={(e) => {
                            const newProjs = [...editConfig.projects];
                            newProjs[idx].title = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Category Tag</label>
                        <input
                          type="text"
                          value={proj.category}
                          onChange={(e) => {
                            const newProjs = [...editConfig.projects];
                            newProjs[idx].category = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">GitHub Repository URL</label>
                        <input
                          type="text"
                          value={proj.github}
                          onChange={(e) => {
                            const newProjs = [...editConfig.projects];
                            newProjs[idx].github = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Live Demo Link</label>
                        <input
                          type="text"
                          value={proj.demo}
                          onChange={(e) => {
                            const newProjs = [...editConfig.projects];
                            newProjs[idx].demo = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="grid grid-cols-2 gap-3">
                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Status</label>
                          <input
                            type="text"
                            value={proj.status}
                            onChange={(e) => {
                              const newProjs = [...editConfig.projects];
                              newProjs[idx].status = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>

                        <div className="flex flex-col justify-center items-center pt-2">
                          <span className="font-mono text-[10px] text-gray-500 uppercase mb-1">Featured</span>
                          <input
                            type="checkbox"
                            checked={proj.featured}
                            onChange={(e) => {
                              const newProjs = [...editConfig.projects];
                              newProjs[idx].featured = e.target.checked;
                              setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                            }}
                            className="w-4.5 h-4.5 text-emerald bg-black/40 border-gray-800 rounded focus:ring-emerald accent-emerald"
                          />
                        </div>
                      </div>
                    </div>

                    {/* Descriptions */}
                    <div className="grid grid-cols-1 gap-4">
                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Short Description Tagline</label>
                        <input
                          type="text"
                          value={proj.shortDesc}
                          onChange={(e) => {
                            const newProjs = [...editConfig.projects];
                            newProjs[idx].shortDesc = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-sans text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Detailed Project Case-Study Description</label>
                        <textarea
                          rows={3}
                          value={proj.longDesc}
                          onChange={(e) => {
                            const newProjs = [...editConfig.projects];
                            newProjs[idx].longDesc = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg p-3 text-xs font-sans text-gray-300 outline-none resize-none"
                        />
                      </div>

                      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Challenges Faced</label>
                          <textarea
                            rows={2}
                            value={proj.challenges || ""}
                            onChange={(e) => {
                              const newProjs = [...editConfig.projects];
                              newProjs[idx].challenges = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg p-3 text-xs font-sans text-gray-300 outline-none resize-none"
                          />
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Learnings & Tech Takeaways</label>
                          <textarea
                            rows={2}
                            value={proj.learnings || ""}
                            onChange={(e) => {
                              const newProjs = [...editConfig.projects];
                              newProjs[idx].learnings = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg p-3 text-xs font-sans text-gray-300 outline-none resize-none"
                          />
                        </div>
                      </div>
                    </div>

                    {/* Tech stack tags */}
                    <div className="space-y-2">
                      <div className="flex justify-between items-center">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Tech Stack Frameworks</label>
                        <button
                          onClick={() => {
                            playClickSound();
                            const newProjs = [...editConfig.projects];
                            newProjs[idx].techStack = [...newProjs[idx].techStack, "Tag"];
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="px-2 py-0.5 bg-emerald/10 border border-emerald/20 rounded text-[9px] font-mono text-emerald flex items-center gap-1"
                        >
                          <Plus className="w-3 h-3" /> Add Tech
                        </button>
                      </div>

                      <div className="flex flex-wrap gap-2">
                        {proj.techStack.map((tech, tIdx) => (
                          <div key={tIdx} className="flex items-center bg-black/60 border border-gray-800 rounded pl-2.5 pr-1.5 py-1">
                            <input
                              type="text"
                              value={tech}
                              onChange={(e) => {
                                const newProjs = [...editConfig.projects];
                                newProjs[idx].techStack[tIdx] = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                              }}
                              className="bg-transparent border-none text-xs font-mono text-white focus:outline-none w-20 p-0"
                            />
                            <button
                              onClick={() => {
                                playClickSound();
                                const newProjs = [...editConfig.projects];
                                newProjs[idx].techStack = newProjs[idx].techStack.filter((_, i) => i !== tIdx);
                                setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                              }}
                              className="ml-2 text-gray-500 hover:text-red-400 p-0.5"
                            >
                              <Trash2 className="w-3 h-3" />
                            </button>
                          </div>
                        ))}
                      </div>
                    </div>

                    {/* Metrics editor */}
                    <div className="space-y-2">
                      <div className="flex justify-between items-center">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Performance Metrics (Key-Value)</label>
                        <button
                          onClick={() => {
                            playClickSound();
                            const newProjs = [...editConfig.projects];
                            const currMetrics = newProjs[idx].metrics || {};
                            newProjs[idx].metrics = { ...currMetrics, "newMetric": "Value" };
                            setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                          }}
                          className="px-2 py-0.5 bg-emerald/10 border border-emerald/20 rounded text-[9px] font-mono text-emerald flex items-center gap-1"
                        >
                          <Plus className="w-3 h-3" /> Add Metric
                        </button>
                      </div>

                      <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-3 bg-black/30 border border-gray-900 rounded-lg p-4">
                        {proj.metrics && Object.keys(proj.metrics).map((mKey) => (
                          <div key={mKey} className="flex gap-2 items-center">
                            <span className="font-mono text-[10px] text-gray-500">{mKey}:</span>
                            <input
                              type="text"
                              value={proj.metrics[mKey as keyof typeof proj.metrics] || ""}
                              onChange={(e) => {
                                const newProjs = [...editConfig.projects];
                                const updatedMetrics = { ...newProjs[idx].metrics };
                                (updatedMetrics as any)[mKey] = e.target.value;
                                newProjs[idx].metrics = updatedMetrics;
                                setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                              }}
                              className="flex-1 bg-black/60 border border-gray-800 focus:border-emerald rounded px-2 py-1 text-xs font-mono text-white outline-none"
                            />
                            <button
                              onClick={() => {
                                playClickSound();
                                const newProjs = [...editConfig.projects];
                                const updatedMetrics = { ...newProjs[idx].metrics };
                                delete (updatedMetrics as any)[mKey];
                                newProjs[idx].metrics = updatedMetrics;
                                setEditConfig(prev => prev ? { ...prev, projects: newProjs } : null);
                              }}
                              className="text-gray-500 hover:text-red-400 p-1"
                            >
                              <Trash2 className="w-3.5 h-3.5" />
                            </button>
                          </div>
                        ))}
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}

          {activeTab === "certs" && (
            <div className="space-y-8">
              
              {/* Certifications Block */}
              <div className="space-y-6">
                <div className="flex justify-between items-center border-b border-gray-800 pb-3">
                  <div>
                    <h3 className="font-mono text-lg font-bold text-emerald">Cryptographic Certifications</h3>
                    <p className="font-sans text-xs text-gray-500 mt-1">
                      Manage cryptographic certificate cards (AWS, Google, Meta) with credential verification links.
                    </p>
                  </div>
                  <button
                    onClick={() => {
                      playClickSound();
                      setEditConfig((prev) => {
                        if (!prev) return null;
                        return {
                          ...prev,
                          certifications: [
                            ...prev.certifications,
                            {
                              title: "Professional Certification Course",
                              issuer: "Credible Issuer Name",
                              date: "2025",
                              credentialId: "CRED-ID-SPEC",
                              credentialUrl: "https://verification.link",
                              pdfUrl: "",
                              skillsGained: ["Key Skill"],
                              badgeColor: "#50c878"
                            }
                          ]
                        };
                      });
                    }}
                    className="px-3 py-1.5 bg-emerald text-black font-pixel text-[10px] uppercase rounded-lg shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
                  >
                    <Plus className="w-3.5 h-3.5" />
                    <span>ADD CERT</span>
                  </button>
                </div>

                <div className="space-y-6">
                  {editConfig.certifications.map((cert, idx) => (
                    <div key={idx} className="relative bg-[#0a0f11] border border-gray-800 rounded-xl p-6 space-y-4">
                      <button
                        onClick={() => {
                          playClickSound();
                          setEditConfig((prev) => {
                            if (!prev) return null;
                            return {
                              ...prev,
                              certifications: prev.certifications.filter((_, i) => i !== idx)
                            };
                          });
                        }}
                        className="absolute top-4 right-4 p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded-lg transition-colors"
                      >
                        <Trash2 className="w-4 h-4" />
                      </button>

                      <h4 className="font-pixel text-[9px] text-emerald uppercase tracking-wider">
                        // CERTIFICATE_{idx + 1}
                      </h4>

                      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Title</label>
                          <input
                            type="text"
                            value={cert.title}
                            onChange={(e) => {
                              const newCerts = [...editConfig.certifications];
                              newCerts[idx].title = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Issuer</label>
                          <input
                            type="text"
                            value={cert.issuer}
                            onChange={(e) => {
                              const newCerts = [...editConfig.certifications];
                              newCerts[idx].issuer = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>

                        <div className="grid grid-cols-2 gap-3">
                          <div className="space-y-1">
                            <label className="font-mono text-[10px] text-gray-500 uppercase">Date</label>
                            <input
                              type="text"
                              value={cert.date}
                              onChange={(e) => {
                                const newCerts = [...editConfig.certifications];
                                newCerts[idx].date = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                              }}
                              className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                            />
                          </div>

                          <div className="space-y-1">
                            <label className="font-mono text-[10px] text-gray-500 uppercase">Badge Color (Hex)</label>
                            <input
                              type="text"
                              value={cert.badgeColor || ""}
                              onChange={(e) => {
                                const newCerts = [...editConfig.certifications];
                                newCerts[idx].badgeColor = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                              }}
                              className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                            />
                          </div>
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Credential ID</label>
                          <input
                            type="text"
                            value={cert.credentialId}
                            onChange={(e) => {
                              const newCerts = [...editConfig.certifications];
                              newCerts[idx].credentialId = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Credential Verification URL</label>
                          <input
                            type="text"
                            value={cert.credentialUrl}
                            onChange={(e) => {
                              const newCerts = [...editConfig.certifications];
                              newCerts[idx].credentialUrl = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>

                        <div className="space-y-1">
                          <div className="flex justify-between items-center">
                            <label className="font-mono text-[10px] text-emerald font-bold uppercase">PDF Certificate path</label>
                            <span className="font-mono text-[8px] text-gray-500">e.g. /certificates/my_cert.pdf</span>
                          </div>
                          <input
                            type="text"
                            value={cert.pdfUrl || ""}
                            placeholder="/certificates/my_certificate.pdf"
                            onChange={(e) => {
                              const newCerts = [...editConfig.certifications];
                              newCerts[idx].pdfUrl = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                            }}
                            className="w-full bg-black/40 border border-emerald/20 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>
                      </div>

                      {/* Skills Gained in Certificate */}
                      <div className="space-y-2 pt-2">
                        <div className="flex justify-between items-center">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Capabilities Verified</label>
                          <button
                            onClick={() => {
                              playClickSound();
                              const newCerts = [...editConfig.certifications];
                              newCerts[idx].skillsGained = [...newCerts[idx].skillsGained, "Skill Tag"];
                              setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                            }}
                            className="px-2 py-0.5 bg-emerald/10 border border-emerald/20 rounded text-[9px] font-mono text-emerald flex items-center gap-1"
                          >
                            <Plus className="w-3 h-3" /> Add Tag
                          </button>
                        </div>

                        <div className="flex flex-wrap gap-2">
                          {cert.skillsGained.map((tag: string, tIdx: number) => (
                            <div key={tIdx} className="flex items-center bg-black/60 border border-gray-800 rounded pl-2.5 pr-1.5 py-1">
                              <input
                                type="text"
                                value={tag}
                                onChange={(e) => {
                                  const newCerts = [...editConfig.certifications];
                                  newCerts[idx].skillsGained[tIdx] = e.target.value;
                                  setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                                }}
                                className="bg-transparent border-none text-xs font-mono text-white focus:outline-none w-24 p-0"
                              />
                              <button
                                onClick={() => {
                                  playClickSound();
                                  const newCerts = [...editConfig.certifications];
                                  newCerts[idx].skillsGained = newCerts[idx].skillsGained.filter((_, i) => i !== tIdx);
                                  setEditConfig(prev => prev ? { ...prev, certifications: newCerts } : null);
                                }}
                                className="ml-2 text-gray-500 hover:text-red-400 p-0.5"
                              >
                                <Trash2 className="w-3 h-3" />
                              </button>
                            </div>
                          ))}
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              </div>

              {/* Achievements Block */}
              <div className="space-y-6 pt-6">
                <div className="flex justify-between items-center border-b border-gray-800 pb-3">
                  <div>
                    <h3 className="font-mono text-lg font-bold text-emerald">Achievements & Medals</h3>
                    <p className="font-sans text-xs text-gray-500 mt-1">
                      Manage awards, coding rankings, and institutional medals.
                    </p>
                  </div>
                  <button
                    onClick={() => {
                      playClickSound();
                      setEditConfig((prev) => {
                        if (!prev) return null;
                        return {
                          ...prev,
                          achievements: [
                            ...prev.achievements,
                            {
                              title: "Award Achievement Title",
                              issuer: "Award Issuer Entity",
                              date: "2025",
                              rarity: "common",
                              desc: "Achievement description summary details.",
                              reward: "Special Title / Medal"
                            }
                          ]
                        };
                      });
                    }}
                    className="px-3 py-1.5 bg-emerald text-black font-pixel text-[10px] uppercase rounded-lg shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
                  >
                    <Plus className="w-3.5 h-3.5" />
                    <span>ADD AWARD</span>
                  </button>
                </div>

                <div className="grid grid-cols-1 gap-6">
                  {editConfig.achievements.map((ach, idx) => (
                    <div key={idx} className="relative bg-[#0a0f11] border border-gray-800 rounded-xl p-6 space-y-4">
                      <button
                        onClick={() => {
                          playClickSound();
                          setEditConfig((prev) => {
                            if (!prev) return null;
                            return {
                              ...prev,
                              achievements: prev.achievements.filter((_, i) => i !== idx)
                            };
                          });
                        }}
                        className="absolute top-4 right-4 p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded-lg transition-colors"
                      >
                        <Trash2 className="w-4 h-4" />
                      </button>

                      <h4 className="font-pixel text-[9px] text-emerald uppercase tracking-wider">
                        // AWARD_{idx + 1}
                      </h4>

                      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Title</label>
                          <input
                            type="text"
                            value={ach.title}
                            onChange={(e) => {
                              const newAchs = [...editConfig.achievements];
                              newAchs[idx].title = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, achievements: newAchs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Issuer</label>
                          <input
                            type="text"
                            value={ach.issuer}
                            onChange={(e) => {
                              const newAchs = [...editConfig.achievements];
                              newAchs[idx].issuer = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, achievements: newAchs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>

                        <div className="grid grid-cols-2 gap-3">
                          <div className="space-y-1">
                            <label className="font-mono text-[10px] text-gray-500 uppercase">Date</label>
                            <input
                              type="text"
                              value={ach.date}
                              onChange={(e) => {
                                const newAchs = [...editConfig.achievements];
                                newAchs[idx].date = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, achievements: newAchs } : null);
                              }}
                              className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                            />
                          </div>

                          <div className="space-y-1">
                            <label className="font-mono text-[10px] text-gray-500 uppercase">Rarity Glow</label>
                            <select
                              value={ach.rarity}
                              onChange={(e) => {
                                const newAchs = [...editConfig.achievements];
                                newAchs[idx].rarity = e.target.value as any;
                                setEditConfig(prev => prev ? { ...prev, achievements: newAchs } : null);
                              }}
                              className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                            >
                              <option value="common">Common (Red)</option>
                              <option value="rare">Rare (Cyan)</option>
                              <option value="epic">Epic (Purple)</option>
                              <option value="legendary">Legendary (Gold)</option>
                            </select>
                          </div>
                        </div>

                        <div className="md:col-span-2 space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Award Description</label>
                          <input
                            type="text"
                            value={ach.desc}
                            onChange={(e) => {
                              const newAchs = [...editConfig.achievements];
                              newAchs[idx].desc = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, achievements: newAchs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-sans text-white outline-none"
                          />
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Reward Title / Medal Type</label>
                          <input
                            type="text"
                            value={ach.reward}
                            onChange={(e) => {
                              const newAchs = [...editConfig.achievements];
                              newAchs[idx].reward = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, achievements: newAchs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          )}

          {activeTab === "experience" && null} {/* Managed under same tab view but tab is experience */}
          
          {activeTab === "certs" && null}

          {activeTab === "blogs" && (
            <div className="space-y-6">
              <div className="flex justify-between items-center">
                <div>
                  <h3 className="font-mono text-lg font-bold text-emerald">Blogs & Technical Scrolls</h3>
                  <p className="font-sans text-xs text-gray-500 mt-1">
                    Compose technical blog entries matching your custom UI scrolls layout.
                  </p>
                </div>
                <button
                  onClick={() => {
                    playClickSound();
                    setEditConfig((prev) => {
                      if (!prev) return null;
                      return {
                        ...prev,
                        blogs: [
                          ...prev.blogs,
                          {
                            slug: "new-scroll-entry",
                            title: "New Enchanted Scroll Article",
                            date: "June 2026",
                            readingTime: "5 min read",
                            tags: ["General"],
                            difficulty: "Intermediate",
                            summary: "Short excerpt summary of the technical write-up.",
                            content: "Detailed markdown document content of the article..."
                          }
                        ]
                      };
                    });
                  }}
                  className="px-3 py-1.5 bg-emerald text-black font-pixel text-[10px] uppercase rounded-lg shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
                >
                  <Plus className="w-3.5 h-3.5" />
                  <span>ADD BLOG</span>
                </button>
              </div>

              <div className="space-y-8">
                {editConfig.blogs.map((blog, idx) => (
                  <div key={idx} className="relative bg-[#0a0f11] border border-gray-800 rounded-xl p-6 space-y-4">
                    <button
                      onClick={() => {
                        playClickSound();
                        setEditConfig((prev) => {
                          if (!prev) return null;
                          return {
                            ...prev,
                            blogs: prev.blogs.filter((_, i) => i !== idx)
                          };
                        });
                      }}
                      className="absolute top-4 right-4 p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded-lg transition-colors"
                    >
                      <Trash2 className="w-4 h-4" />
                    </button>

                    <h4 className="font-pixel text-[9px] text-emerald uppercase tracking-wider">
                      // BLOG_SCROLL_{idx + 1}
                    </h4>

                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Slug ID</label>
                        <input
                          type="text"
                          value={blog.slug}
                          onChange={(e) => {
                            const newBlogs = [...editConfig.blogs];
                            newBlogs[idx].slug = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Title</label>
                        <input
                          type="text"
                          value={blog.title}
                          onChange={(e) => {
                            const newBlogs = [...editConfig.blogs];
                            newBlogs[idx].title = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="grid grid-cols-2 gap-3">
                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Difficulty</label>
                          <select
                            value={blog.difficulty}
                            onChange={(e) => {
                              const newBlogs = [...editConfig.blogs];
                              newBlogs[idx].difficulty = e.target.value as any;
                              setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          >
                            <option value="Beginner">Beginner</option>
                            <option value="Intermediate">Intermediate</option>
                            <option value="Advanced">Advanced</option>
                          </select>
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Read Time</label>
                          <input
                            type="text"
                            value={blog.readingTime}
                            onChange={(e) => {
                              const newBlogs = [...editConfig.blogs];
                              newBlogs[idx].readingTime = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Publish Date</label>
                        <input
                          type="text"
                          value={blog.date}
                          onChange={(e) => {
                            const newBlogs = [...editConfig.blogs];
                            newBlogs[idx].date = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>
                    </div>

                    <div className="space-y-1">
                      <label className="font-mono text-[10px] text-gray-500 uppercase">Summary Line</label>
                      <input
                        type="text"
                        value={blog.summary}
                        onChange={(e) => {
                          const newBlogs = [...editConfig.blogs];
                          newBlogs[idx].summary = e.target.value;
                          setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                        }}
                        className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-sans text-gray-300 outline-none"
                      />
                    </div>

                    <div className="space-y-1">
                      <label className="font-mono text-[10px] text-gray-500 uppercase">Full Markdown Content</label>
                      <textarea
                        rows={6}
                        value={blog.content}
                        onChange={(e) => {
                          const newBlogs = [...editConfig.blogs];
                          newBlogs[idx].content = e.target.value;
                          setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                        }}
                        className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg p-3 text-xs font-mono text-gray-300 outline-none resize-y"
                      />
                    </div>

                    {/* Blog tag list editor */}
                    <div className="space-y-2">
                      <div className="flex justify-between items-center">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Article Categories/Tags</label>
                        <button
                          onClick={() => {
                            playClickSound();
                            const newBlogs = [...editConfig.blogs];
                            newBlogs[idx].tags = [...newBlogs[idx].tags, "Topic"];
                            setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                          }}
                          className="px-2 py-0.5 bg-emerald/10 border border-emerald/20 rounded text-[9px] font-mono text-emerald flex items-center gap-1"
                        >
                          <Plus className="w-3 h-3" /> Add Tag
                        </button>
                      </div>

                      <div className="flex flex-wrap gap-2">
                        {blog.tags.map((tag, tIdx) => (
                          <div key={tIdx} className="flex items-center bg-black/60 border border-gray-800 rounded pl-2.5 pr-1.5 py-1">
                            <input
                              type="text"
                              value={tag}
                              onChange={(e) => {
                                const newBlogs = [...editConfig.blogs];
                                newBlogs[idx].tags[tIdx] = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                              }}
                              className="bg-transparent border-none text-xs font-mono text-white focus:outline-none w-20 p-0"
                            />
                            <button
                              onClick={() => {
                                playClickSound();
                                const newBlogs = [...editConfig.blogs];
                                newBlogs[idx].tags = newBlogs[idx].tags.filter((_, i) => i !== tIdx);
                                setEditConfig(prev => prev ? { ...prev, blogs: newBlogs } : null);
                              }}
                              className="ml-2 text-gray-500 hover:text-red-400 p-0.5"
                            >
                              <Trash2 className="w-3 h-3" />
                            </button>
                          </div>
                        ))}
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}

          {activeTab === "gallery" && (
            <div className="space-y-6">
              <div className="flex justify-between items-center">
                <div>
                  <h3 className="font-mono text-lg font-bold text-emerald">Voxel Memory Gallery</h3>
                  <p className="font-sans text-xs text-gray-500 mt-1">
                    Manage visual media polaroids and holograms shown in the museum layout.
                  </p>
                </div>
                <button
                  onClick={() => {
                    playClickSound();
                    setEditConfig((prev) => {
                      if (!prev) return null;
                      return {
                        ...prev,
                        gallery: [
                          ...prev.gallery,
                          {
                            id: "new-gallery-item",
                            type: "polaroid",
                            title: "New Polaroid Event Memory",
                            category: "College",
                            date: "2026",
                            image: "/gallery/new.jpg",
                            tags: ["Activity"],
                            desc: "Detailed memory description caption text."
                          }
                        ]
                      };
                    });
                  }}
                  className="px-3 py-1.5 bg-emerald text-black font-pixel text-[10px] uppercase rounded-lg shadow-[2px_2px_0_#000] active:translate-y-0.5 flex items-center gap-1.5"
                >
                  <Plus className="w-3.5 h-3.5" />
                  <span>ADD ITEM</span>
                </button>
              </div>

              <div className="grid grid-cols-1 gap-6">
                {editConfig.gallery.map((item, idx) => (
                  <div key={idx} className="relative bg-[#0a0f11] border border-gray-800 rounded-xl p-6 space-y-4">
                    <button
                      onClick={() => {
                        playClickSound();
                        setEditConfig((prev) => {
                          if (!prev) return null;
                          return {
                            ...prev,
                            gallery: prev.gallery.filter((_, i) => i !== idx)
                          };
                        });
                      }}
                      className="absolute top-4 right-4 p-2 border border-red-500/20 hover:border-red-500 hover:bg-red-500/5 text-red-400 rounded-lg transition-colors"
                    >
                      <Trash2 className="w-4 h-4" />
                    </button>

                    <h4 className="font-pixel text-[9px] text-emerald uppercase tracking-wider">
                      // GALLERY_ITEM_{idx + 1}
                    </h4>

                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Unique ID</label>
                        <input
                          type="text"
                          value={item.id}
                          onChange={(e) => {
                            const newGals = [...editConfig.gallery];
                            newGals[idx].id = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Title</label>
                        <input
                          type="text"
                          value={item.title}
                          onChange={(e) => {
                            const newGals = [...editConfig.gallery];
                            newGals[idx].title = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="grid grid-cols-2 gap-3">
                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Card Style Render</label>
                          <select
                            value={item.type}
                            onChange={(e) => {
                              const newGals = [...editConfig.gallery];
                              newGals[idx].type = e.target.value as any;
                              setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          >
                            <option value="polaroid">Polaroid (Tape)</option>
                            <option value="hologram">Hologram (Scanlines)</option>
                            <option value="memory">Default Glassmorphic</option>
                          </select>
                        </div>

                        <div className="space-y-1">
                          <label className="font-mono text-[10px] text-gray-500 uppercase">Category Filter</label>
                          <input
                            type="text"
                            value={item.category}
                            onChange={(e) => {
                              const newGals = [...editConfig.gallery];
                              newGals[idx].category = e.target.value;
                              setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                            }}
                            className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                          />
                        </div>
                      </div>

                      <div className="space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Date</label>
                        <input
                          type="text"
                          value={item.date}
                          onChange={(e) => {
                            const newGals = [...editConfig.gallery];
                            newGals[idx].date = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>

                      <div className="md:col-span-2 space-y-1">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Image File URL</label>
                        <input
                          type="text"
                          value={item.image}
                          onChange={(e) => {
                            const newGals = [...editConfig.gallery];
                            newGals[idx].image = e.target.value;
                            setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                          }}
                          className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg px-3 py-1.5 text-xs font-mono text-white outline-none"
                        />
                      </div>
                    </div>

                    <div className="space-y-1">
                      <label className="font-mono text-[10px] text-gray-500 uppercase">Caption Description</label>
                      <textarea
                        rows={2}
                        value={item.desc}
                        onChange={(e) => {
                          const newGals = [...editConfig.gallery];
                          newGals[idx].desc = e.target.value;
                          setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                        }}
                        className="w-full bg-black/40 border border-gray-800 focus:border-emerald rounded-lg p-3 text-xs font-sans text-gray-300 outline-none resize-none"
                      />
                    </div>

                    {/* Tag sub-list editor */}
                    <div className="space-y-2">
                      <div className="flex justify-between items-center">
                        <label className="font-mono text-[10px] text-gray-500 uppercase">Image Pins/Tags</label>
                        <button
                          onClick={() => {
                            playClickSound();
                            const newGals = [...editConfig.gallery];
                            newGals[idx].tags = [...newGals[idx].tags, "Pin"];
                            setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                          }}
                          className="px-2 py-0.5 bg-emerald/10 border border-emerald/20 rounded text-[9px] font-mono text-emerald flex items-center gap-1"
                        >
                          <Plus className="w-3.5 h-3.5" /> Add Tag
                        </button>
                      </div>

                      <div className="flex flex-wrap gap-2">
                        {item.tags.map((tag, tIdx) => (
                          <div key={tIdx} className="flex items-center bg-black/60 border border-gray-800 rounded pl-2.5 pr-1.5 py-1">
                            <input
                              type="text"
                              value={tag}
                              onChange={(e) => {
                                const newGals = [...editConfig.gallery];
                                newGals[idx].tags[tIdx] = e.target.value;
                                setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                              }}
                              className="bg-transparent border-none text-xs font-mono text-white focus:outline-none w-20 p-0"
                            />
                            <button
                              onClick={() => {
                                playClickSound();
                                const newGals = [...editConfig.gallery];
                                newGals[idx].tags = newGals[idx].tags.filter((_, i) => i !== tIdx);
                                setEditConfig(prev => prev ? { ...prev, gallery: newGals } : null);
                              }}
                              className="ml-2 text-gray-500 hover:text-red-400 p-0.5"
                            >
                              <Trash2 className="w-3 h-3" />
                            </button>
                          </div>
                        ))}
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}

          {/* Tips / Instructions Banner Footer */}
          <footer className="mt-12 border-t border-gray-800 pt-6 flex items-start gap-3 text-gray-500">
            <HelpCircle className="w-5 h-5 text-emerald shrink-0 mt-0.5" />
            <div className="space-y-1">
              <span className="font-mono text-[10px] text-gray-400 uppercase font-bold">Admin Console Guidelines</span>
              <p className="font-sans text-[11px] leading-relaxed">
                Applying changes saves them immediately to your browser&apos;s local storage database for instant live previews.
                To save permanently across devices and server deployments, download the configuration file using <strong className="text-emerald">EXPORT TS</strong> and save it as <code className="text-purple-glow font-mono font-semibold bg-purple-glow/5 border border-purple-glow/10 px-1 rounded">src/config/userConfig.ts</code>.
              </p>
            </div>
          </footer>

        </main>
      </div>

      {/* Floating Success Toast messages */}
      {toastMessage && (
        <div className={`fixed bottom-6 right-6 z-50 px-6 py-4 rounded-xl border font-mono text-xs shadow-2xl flex items-center gap-3 animate-bounce ${
          toastType === "success"
            ? "bg-emerald/10 border-emerald text-emerald shadow-emerald/15"
            : toastType === "info"
            ? "bg-cyan-glow/10 border-cyan-glow text-cyan-glow shadow-cyan-glow/15"
            : "bg-red-500/10 border-red-500 text-red-500 shadow-red-500/15"
        }`}>
          <div className={`w-2 h-2 rounded-full animate-ping ${
            toastType === "success" ? "bg-emerald" : toastType === "info" ? "bg-cyan-glow" : "bg-red-500"
          }`} />
          <span>{toastMessage}</span>
        </div>
      )}

    </div>
  );
}
